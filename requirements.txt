# Core data handling
pandas==2.3.3
numpy>=2.2  # unpinned from 2.2.6 -- that exact build has no Python 3.14 wheel
            # on Windows and was forcing a from-source build that needs a
            # newer GCC than this machine has. Any 2.2+ wheel works fine here.

# Stage 1 retrieval (sparse/BM25)
rank-bm25==0.2.2

# Stage 3/4 feature scoring + learned reranker
scikit-learn==1.7.2

# lightgbm is deliberately NOT listed here. src/train_ranker.py tries it first
# and falls back to sklearn's GradientBoostingRegressor when it's absent or
# fails at fit time (see experiments/exp_log.md #9 for the numpy>=2.0 crash we
# reproduced). The committed submission.csv was produced by the sklearn
# backend; pinning lightgbm would risk the grading sandbox resolving a version
# that fits successfully and produces a *different* top 100 than the one we
# submitted -- a reproduction mismatch at Stage 3. Deterministic beats
# marginally fancier. (This file previously ended mid-comment -- a truncation
# we found during the Docker clean-room test and fixed here.)